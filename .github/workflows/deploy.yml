name: deploy
on:
  push:
    branches:
      - '*'
      - '!master'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/zeit-now@master
        env:
          ZEIT_TOKEN: ${{ secrets.ZEIT_TOKEN }}
